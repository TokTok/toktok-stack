load("@rules_cc//cc:defs.bzl", "cc_library")
load("@rules_zig//zig:defs.bzl", "zig_test")

cc_library(
    name = "clib",
    srcs = ["csource.c"],
    hdrs = ["csource.h"],
)

zig_test(
    name = "testsuite",
    size = "small",
    cdeps = [
        ":clib",
        "@rules_zig//zig/lib:libc",
    ],
    main = "main.zig",
)
