# Pre-build //third_party in release mode for speedy PR builds on sub-repos.
FROM toxchat/toktok-stack:$VERSION
RUN ["bazel", "build", "-k", \
 "--config=remote", \
 "--config=release", \
 "--", \
 "//third_party/..."]
